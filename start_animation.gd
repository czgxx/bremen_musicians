extends Node2D

# 预加载场景（可以放在脚本顶部）
#var game_scene = preload("res://tscn/card_manager.tscn")

# 需要切换时调用


func _on_button_button_down() -> void:
	#get_tree().change_scene_to_packed(game_scene)
	pass # Replace with function body.
